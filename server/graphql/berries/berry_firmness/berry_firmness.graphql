type Query {
  berryFirmness(id: Int, name: String): BerryFirmness
  berryFirmnesses(perPage: Int!, page: Int!): [BerryFirmness]
}

type BerryFirmness {
  id: Int!
  name: String
  berries: [Berry]
  names: [Name]
}